{
  "scripts": {
    "start": "cd website && docusaurus-start",
    "build": "cd website && docusaurus-build",
    "deploy": "cd website && docusaurus-publish"
  },
  "devDependencies": {
    "docusaurus": "^1.14.7"
  }
}
